
## Rules
1. CRITICAL: Every time you update JavaScript (.js) or CSS (.css) files and bump their cache buster version (e.g. `?v=...`), you MUST explicitly state the new version number in your response to the user. The user relies on this version number to verify their browser cache is cleared. Failure to do so will cause severe disruption.
