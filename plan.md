# เพิ่มความสามารถในการแก้ไขทั้ง "เวลาตาราง" และ "เวลาจริง"

เนื่องจากปัจจุบันหน้าต่างแก้ไขเวลามีแค่ 2 ช่อง (เวลาเข้า/เวลาออก) ซึ่งเคยถูกกำหนดให้ใช้แทน **เวลาตาราง** (เพื่อเปลี่ยนกะการทำงาน) แต่ตอนนี้เราต้องการให้แอดมินสามารถกำหนด **เวลาจริง** ที่พนักงานมาถึง (กรณีลืมสแกน แต่มาสาย) ได้ด้วย เพื่อให้ระบบคำนวณการมาสายและหักเงินได้ถูกต้อง

## แผนการแก้ไข (Proposed Changes)

1. **เพิ่มช่องกรอกข้อมูลในหน้าต่างแก้ไข (Modal)**
   - แบ่งเป็น 2 ส่วนชัดเจน คือ
     1. **เวลาตามตาราง (Scheduled Time):** เวลาเข้า - เวลาออก
     2. **เวลาเข้างานจริง (Actual Time):** เวลาเข้า - เวลาออก

2. **ปรับปรุงฐานข้อมูล (Google Apps Script)**
   - เพิ่มคอลัมน์ใหม่ในชีต "Logs" เพื่อเก็บ "เวลาเข้าจริง" และ "เวลาออกจริง" ที่แอดมินเป็นคนกรอกแยกต่างหากจากเวลาตาราง
   - อัปเดต API ให้รับและบันทึกข้อมูลทั้ง 4 ช่องได้

3. **ปรับปรุงการแสดงผลและคำนวณ (UI & Logic)**
   - **เวลาตาราง:** หากแอดมินมีการแก้ไข จะแสดงเป็น **ตัวหนา (ใหญ่)** แทนที่เวลาตารางเดิม
   - **เวลาจริง:** หากแอดมินมีการแก้ไข จะแสดงเป็นตัวเล็กด้านล่าง แทนที่เครื่องหมาย `-` (และจะลบการแจ้งเตือนกล่องแดง "ไม่มีเข้างาน/ออกงาน" ออก)
   - **การคำนวณมาสาย:** ระบบจะนำ "เวลาจริงที่แอดมินกรอก" ไปเปรียบเทียบกับ "เวลาตาราง" เพื่อคำนวณว่าสายกี่นาที และหักชั่วโมงตามจริงอย่างถูกต้อง

## คำถาม (Open Questions)
แผนการนี้จะตรงกับที่คุณต้องการไหมครับ? คือแอดมินจะมีอิสระเลยว่า จะแก้แค่กะการทำงาน (ตาราง) หรือจะแก้แค่เวลาสแกน (เวลาจริง) หรือจะแก้ทั้งสองอย่างเลยก็ได้ครับ
